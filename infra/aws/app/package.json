{
  "name": "milte-aws-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@aws-sdk/client-cognito-identity-provider": "^3.1113.0",
    "@aws-sdk/client-dynamodb": "^3.1113.0",
    "@aws-sdk/client-geo-places": "^3.1113.0",
    "@aws-sdk/lib-dynamodb": "^3.1113.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.152",
    "@types/node": "^24.3.0",
    "esbuild": "^0.28.2",
    "typescript": "~6.0.3",
    "vitest": "^3.2.4"
  }
}
