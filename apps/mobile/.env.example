# Copy to .env — values from `npx supabase start` / `npx supabase status`
# NOTE: use your machine's LAN IP instead of 127.0.0.1 when testing on a phone.
EXPO_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-local-anon-key
